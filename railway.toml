[build]
  builder = "DOCKERFILE"
  dockerfilePath = "packages/api/Dockerfile"

[deploy]
  healthcheckPath = "/api/workouts"
  healthcheckTimeout = 30
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
